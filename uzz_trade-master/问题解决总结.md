# 服务器启动问题解决总结

## 🔍 发现的问题

### 问题 1: 未激活虚拟环境
**错误信息**:
```
ImportError: Couldn't import Django. Are you sure it's installed and available on your PYTHONPATH environment variable? Did you forget to activate a virtual environment?
```

**原因**: 直接使用系统的 Python，而不是虚拟环境中的 Python

**解决方案**: 需要先激活虚拟环境

---

### 问题 2: 缺少依赖包
**错误信息**:
```
ModuleNotFoundError: No module named 'corsheaders'
```

**原因**: 虚拟环境中缺少部分依赖包

**解决方案**: 安装 requirements.txt 中的所有依赖

---

## ✅ 解决步骤

### 1. 激活虚拟环境

**PowerShell**:
```powershell
cd d:\uzz-trade\uzz_trade-master
.\venv\Scripts\Activate.ps1
```

**CMD**:
```cmd
cd d:\uzz-trade\uzz_trade-master
venv\Scripts\activate.bat
```

**或者使用批处理文件**:
- 双击 `启动服务器.bat`（会自动激活并启动）

---

### 2. 安装依赖包

```powershell
# 激活虚拟环境后
pip install -r requirements.txt
```

**已安装的包**:
- ✅ django-cors-headers
- ✅ PyMySQL
- ✅ redis
- ✅ 以及其他所有依赖

---

### 3. 验证配置

```powershell
# 检查系统配置
python manage.py check
```

应该显示: `System check identified no issues (0 silenced).`

---

## 🚀 启动服务器

### 方法一：使用批处理文件（最简单）
**双击**: `启动服务器.bat`

### 方法二：手动启动
```powershell
# 1. 激活虚拟环境
.\venv\Scripts\Activate.ps1

# 2. 启动服务器
python manage.py runserver
```

### 方法三：使用 PowerShell 脚本
```powershell
.\启动服务器.ps1
```

---

## 📋 完整启动流程

```powershell
# 1. 进入项目目录
cd d:\uzz-trade\uzz_trade-master

# 2. 激活虚拟环境
.\venv\Scripts\Activate.ps1

# 3. 验证 Django
python -c "import django; print('Django version:', django.get_version())"

# 4. 检查配置
python manage.py check

# 5. 启动服务器
python manage.py runserver
```

---

## ✅ 成功标志

启动成功后，你会看到：
```
Starting development server at http://127.0.0.1:8000/
Quit the server with CTRL-BREAK.
```

---

## ⚠️ 重要提示

1. **每次启动前都要激活虚拟环境**
2. **激活后命令行前会显示 `(venv)`**
3. **如果 PowerShell 执行策略限制，运行**:
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```

---

## 📁 已创建的文件

1. `启动服务器.bat` - Windows 批处理启动脚本
2. `启动服务器.ps1` - PowerShell 启动脚本
3. `虚拟环境激活说明.md` - 详细说明文档
4. `问题解决总结.md` - 本文档

---

## 🎉 现在可以正常启动了！

所有问题已解决，服务器应该可以正常启动。





