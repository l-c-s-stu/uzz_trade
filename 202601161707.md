# Cursor 改造指南：校园二手交易平台 (后端业务补全篇)

## 📂 项目背景与现状
**当前环境**：
- **框架**：Django 5.x + Django REST Framework
- **数据库**：MySQL (已部署上线，有存量数据)
- **前端**：原生 HTML/JS
- **目标**：将现有雏形升级为符合《毕业设计开题报告》要求的完整系统。

**请注意**：由于数据库已部署，所有的模型修改必须通过 `makemigrations` 和 `migrate` 进行，请在修改模型后提醒我执行迁移命令。

---

## 🚀 任务清单 (请按顺序执行，每完成一步暂停等待确认)

请作为一名资深的 Python Django 后端工程师，协助我完成以下 5 个核心任务的改造。

### 1️⃣ 任务一：用户身份认证升级 (Users App)
**目标**：落实开题报告中的“学号实名认证”与“学院信息”。
**涉及文件**：`users/models.py`, `users/serializers.py`

**具体指令**：
1.  **修改 `User` 模型**：
    - 继承自 `AbstractUser` (假设目前已继承)。
    - 新增字段 `student_id`：`CharField`, max_length=20, null=True, blank=True, verbose_name="学号"。**注意**：虽然逻辑上唯一，但为了兼容老数据，先不要设 `unique=True`，或者处理好默认值。
    - 新增字段 `college`：`CharField`, max_length=50, null=True, blank=True, verbose_name="学院"。
2.  **更新 `UserRegSerializer`**：
    - 在注册接口中，增加对 `student_id` 的正则校验（仅允许数字）。
    - 注册时将 `student_id` 和 `college` 设为必填项（可选，根据需求）。

---

### 2️⃣ 任务二：商品分类体系与多图上传 (Goods App)
**目标**：落实“多级分类”与“多图展示”。
**涉及文件**：`goods/models.py`, `goods/serializers.py`, `goods/views.py`, `goods/filters.py`

**具体指令**：
1.  **新建 `Category` 模型**：
    - 字段：`name` (名称), `code` (编码), `parent` (ForeignKey 指向 self, null=True, blank=True, related_name='sub_cat', verbose_name="父类级别")。
    - 实现 `__str__` 方法返回名称。
2.  **修改 `Goods` 模型**：
    - 新增 `category` 外键，关联到 `Category` 模型。
    - 新增 `goods_sn` (商品唯一货号)，可覆写 `save` 方法自动生成。
    - 新增 `status` 字段：`choices=((1, '在售'), (2, '已出'), (3, '下架'))`，默认为 1。
3.  **新建 `GoodsImage` 模型**：
    - 字段：`goods` (ForeignKey 指向 Goods), `image` (ImageField), `add_time`。
    - 用于支持一个商品对应多张详情图。
4.  **更新 Serializer 与 View**：
    - `GoodsSerializer` 需支持读取 `Category` 信息。
    - 增加 `GoodsImageSerializer` 并嵌套在商品详情中。

---

### 3️⃣ 任务三：核心交易模块 (Trade App)
**目标**：落实“订单流转”与“库存管理”，这是毕设的核心难点。
**涉及文件**：`trade/models.py`, `trade/serializers.py`, `trade/views.py`

**具体指令**：
1.  **创建 `Order` (订单) 模型**：
    - 字段：`user` (买家), `goods` (商品), `order_sn` (订单号, unique), `trade_no` (支付流水号, null=True), `pay_status` (待支付/已支付/已取消), `post_script` (留言), `order_mount` (金额), `pay_time`, `add_time`。
2.  **编写 `OrderSerializer`**：
    - 自动生成 `order_sn` (建议格式：当前时间+用户ID+随机数)。
    - 验证逻辑：下单时检查 `goods.status` 是否为“在售”，如果不是则抛出 `ValidationError`。
3.  **编写 `OrderViewSet`**：
    - 继承 `mixins.CreateModelMixin`, `mixins.ListModelMixin`, `mixins.RetrieveModelMixin`, `viewsets.GenericViewSet`。
    - **关键逻辑**：在 `perform_create` 或 `create` 中使用 `transaction.atomic()` 开启事务。
    - 下单成功后，暂时不需要减库存（因为是二手孤品），但需要将商品状态标记为“交易中”或在支付成功后标记为“已出”。

---

### 4️⃣ 任务四：Redis 缓存加速 (Performance)
**目标**：落实“Redis 缓存热点数据”。
**涉及文件**：`settings.py`, `goods/views.py`

**具体指令**：
1.  **配置 Settings**：检查 `CACHES` 配置是否指向 Redis (127.0.0.1:6379)。
2.  **缓存列表页**：
    - 使用 `django.utils.decorators.method_decorator` 和 `cache_page`。
    - 或者使用 `drf-extensions` 的 `CacheResponseMixin`。
    - 对 `/api/goods/` 列表接口进行 60 秒缓存。

---

### 5️⃣ 任务五：后台管理系统 (Admin)
**目标**：落实“可视化后台管理”。
**涉及文件**：`*/admin.py`

**具体指令**：
1.  在 `users`, `goods`, `trade` 的 `admin.py` 中注册所有模型。
2.  使用 `@admin.register`。
3.  配置 `list_display` (显示关键列), `search_fields` (支持搜索), `list_filter` (支持过滤)。
    - 例如订单后台要能筛选“支付状态”。

---

## ⚠️ 执行原则
1.  **分步进行**：请每次只完成一个任务块，并询问我是否继续。
2.  **代码解释**：生成代码后，请简要解释修改了什么，以及为什么这样符合开题报告。
3.  **迁移提醒**：涉及 `models.py` 修改时，务必提醒我运行 `python manage.py makemigrations` 和 `python manage.py migrate`。
